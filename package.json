{
  "name": "redux-socket-chat-server",
  "version": "0.1.3",
  "engines": {
    "node": "6.9.1"
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ]
  },
  "dependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "css-loader": "^0.26.0",
    "express": "^4.14.0",
    "foreman": "^2.0.0",
    "fs": "0.0.2",
    "node-sass": "^3.4.2",
    "sass-loader": "^4.0.2",
    "style-loader": "^0.13.0"
  },
  "scripts": {
    "start": "nf start -p 3000",
    "dev": "nf start -p 3000 --procfile Procfile.dev",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "server": "./node_modules/.bin/babel-node server.js"
  }
}
